spring:
  application:
    name: arka-backend
  profiles:
    active: dev

management:
  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: "health, info"
  endpoint:
    health:
      show-details: "always"
      probes:
        enabled: true